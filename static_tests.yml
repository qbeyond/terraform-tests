jobs:
  - job: checkout
    steps:
    - checkout: self
  - job: run_tflint
    steps: 
    - task: CmdLine@2
      inputs: 
        script: |
          echo Curl tflint

          curl -s https://raw.githubusercontent.com/terraform-linters/tflint/master/install_linux.sh | bash
    - task: CmdLine@2
      inputs:
        script: |
          tflint --recursive